#include <stdio.h>
#define FREEZING 32
#define BOILING 212

double f_to_c(int f){
  double c = f - 32;
  c = f * (5/9);
  return c;
}

main(){
  double c;
  int f

    printf("%s\t%s", "Fahrenheit", "Celsius");

  for (f = FREEZING; f <= BOILING; f += 10){
    printf("%d\t%.1f", f, f_to_c(f);
  }
  
}
